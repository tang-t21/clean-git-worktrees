interface:
  display_name: "Clean Git Worktrees"
  short_description: "Audit and safely prune Git worktrees"
  default_prompt: "Use $clean-git-worktrees to audit this repository and remove only clean worktrees backed by merged PRs or no-PR tips already contained in the base."
