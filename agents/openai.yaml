interface:
  display_name: "Clean Git Worktrees"
  short_description: "Review and safely clean local Git worktrees"
  default_prompt: "Use $clean-git-worktrees to list every local worktree, delete only obvious safe cases, and summarize what each retained worktree is doing."
